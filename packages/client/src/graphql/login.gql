mutation login($userName:String!, $password:String!){
    login(
        loginInput : {
            userName:$userName,
            password:$password
        }
    ){
        userName
        password
    }
}